# 0.1.0

- TODO(WP8): replace this placeholder with the 0.1.0 release notes. The adapter fronts Microsoft's
  `roslyn-language-server`, pinned at **5.12.0-1.26426.8**; that version string is part of the
  release notes because it is what a user actually runs, and a bump to it is a user-visible change
  even when nothing in this repository moves. The first line of this file is the version authority —
  the Fallout build parses it in `OnBuildInitialized` and a `# Changelog` title would abort the
  build — so the `# 0.1.0` heading above stays exactly as it is.
