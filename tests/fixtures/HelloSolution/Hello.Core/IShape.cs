namespace Hello.Core;

/// <summary>Something with an area. Two implementations, so goToImplementation has two answers.</summary>
public interface IShape
{
    /// <summary>The area.</summary>
    double Area();
}
